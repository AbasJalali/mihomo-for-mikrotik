# 🚀 نصب خودکار با قوانین ایرانMihomo (Clash) روی Mikrotik RouterOS

این مخزن شامل فایل‌ها و اسکریپت‌های لازم برای نصب و راه‌اندازی کانتینر **Mihomo (هسته Clash)** به صورت خودکار و بهینه روی روترهای میکروتیک با معماری **ARM64** است که میتواند پروتکلهای vless را اجرا کند همچنین از ساب هم پشتیبانی میکند.

---

## 🎯 ۱. ویژگی‌های اصلی پروژه

* **نصب یکپارچه:** نصب و پیکربندی شبکه تنها با یک فرمان در Terminal میکروتیک.
* **تزریق متغیرها:** تزریق خودکار تمامی متغیرهای محیطی ثابت (مانند `HWID`، `User-Agent` و آدرس سابسکریپشن) به کانتینر.
* **ایمیج داخلی:** استفاده مستقیم از فایل ایمیج فشرده شده (`mihomoarm64.tar.gz`) موجود در مخزن، به جای دانلود از Docker Hub.
* **حذف WARP:** عدم وابستگی به کانفیگ‌های WARP برای راه‌اندازی.

---

## 🛠️ ۲. فایل‌های کلیدی

| نام فایل | عملکرد |
| :--- | :--- |
| **`mikrotik-setup-mihomo.rsc`** | **اسکریپت نهایی نصب:** تمام مراحل نصب، تنظیم شبکه، لود ایمیج و راه‌اندازی را به صورت خودکار اجرا می‌کند. |
| **`mihomoarm64.tar.gz`** | فایل ایمیج کانتینر Mihomo (برای معماری ARM64) که لود می‌شود. |
| **`entrypoint.sh`** | اسکریپت سفارشی راه‌اندازی کانتینر که به عنوان Entrypoint به کانتینر تزریق می‌شود. |

---

## ⚙️ ۳. پیش‌نیازها و ویرایش متغیرها

**بسیار مهم:** قبل از اجرای فرمان نصب، باید متغیرهای مورد نیاز (مانند رمز عبور و لینک سابسکریپشن) را در **اسکریپت نصب** ویرایش و ذخیره کنید:

1.  در همین مخزن، فایل **`mikrotik-setup-mihomo.rsc`** را باز کرده و روی دکمه **Edit** کلیک کنید.
2.  مقادیر زیر را در ابتدای اسکریپت پیدا کرده و با اطلاعات واقعی خود جایگزین نمایید:

```routeros
:local UI_SECRET "YOUR_PASSWORD_HERE"           
:local SUB_ADDRESS "https://YOUR_SUB_ADDRESS_HERE" 
```

---

## 🚀 ۴. دستور نصب نهایی (اجرای یک مرحله‌ای)

پس از ویرایش و ذخیره (Commit) فایل `mikrotik-setup-mihomo.rsc`، کافی است این فرمان را به صورت کامل در **Terminal میکروتیک** خود کپی و اجرا کنید:

```routeros
/import url="[https://raw.githubusercontent.com/AbasJalali/mihomo-for-mikrotik/main/mikrotik-setup-mihomo.rsc](https://raw.githubusercontent.com/AbasJalali/mihomo-for-mikrotik/main/mikrotik-setup-mihomo.rsc)"
```

---

## 🌐 ۵. اطلاعات اتصال و بررسی نهایی

پس از اجرای موفقیت‌آمیز اسکریپت و بالا آمدن کانتینر (حدود ۱ دقیقه)، می‌توانید از طریق آدرس زیر به پنل مدیریتی Mihomo دسترسی پیدا کنید:

* **آدرس پنل وب:** `http://172.17.0.2:9090`
* **رمز عبور:** (همان مقداری که برای `UI_SECRET` تنظیم کرده‌اید)

برای مشاهده لاگ‌های کانتینر و اطمینان از صحت عملکرد آن، از این دستور استفاده کنید:

```routeros
/container logs MIHOMO
```
